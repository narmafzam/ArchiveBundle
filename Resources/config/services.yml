imports:
    - { resource: 'menu.yml' }
    - { resource: 'contract.yml' }
    - { resource: 'document.yml' }
    - { resource: 'letter.yml' }

parameters:
    upload_directory: '%kernel.project_dir%/var/uploads'

services:
